import pyautogui as s
import time

s.press("win")
time.sleep(1)

# s.write("notepad", interval = 0.2)
# s.press("enter")

# time.sleep(1)

# s.write("Hello World", interval= 0.2)
# s.press("enter")
# s.write("You did it...", interval=0.1)
# s.press("enter")

s.write("comet", interval=0.1)
s.press("enter")
time.sleep(1)
s.write("you tube", interval=0.3)
s.press("enter",interval=5)
s.write("open yt", interval=0.2)
s.press("enter")
